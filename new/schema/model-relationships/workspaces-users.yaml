from:
  slug: workspaces
  mergeColumns: false
to:
  slug: users
  mergeColumns: false
cardinality: 1:many
mapping:
  - fromColumn: id
    toColumn: workspace_id
  - fromColumn: is_approved
    toColumn: is_admin
